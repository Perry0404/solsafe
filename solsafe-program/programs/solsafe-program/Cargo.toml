[package]
name = "solsafe-program"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
anchor-lang = "0.30.1"
anchor-spl = "0.30.1"
spl-token = "4.0"
# TODO: Add switchboard-v2 when compatible version is available for VRF integration
# switchboard-v2 = "0.5.0"